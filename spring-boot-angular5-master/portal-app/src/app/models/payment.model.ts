import { logging } from 'protractor';

export class Payment{

  id:number;
  txnType: string;
  amount:number;
  remarks: string;
  mentorId:string;
  trainingId:number;
  paymentType:String;
  userId:String;
}
